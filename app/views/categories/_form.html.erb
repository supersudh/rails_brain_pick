<%= render 'shared/errors', obj: @category %>
<%= form_with(model: @category, local: true, class: "shadow p-3 mb-3 bg-info rounded", html: { autocomplete: "nope" }) do |form| %>

  <div class="form-group row">
    <%= form.label :name, "Category name", class: "col-2 col-form-label text-light" %>
    <div class="col-10">
      <%= form.text_field :name, class: "form-control shadow rounded", placeholder: "Enter a name", autocomplete: "nope" %>
    </div>
  </div>

  <div class="form-group row justify-content-center">
    <%= form.submit class: "btn btn-outline-light btn-lg" %>
  </div>
<% end %>